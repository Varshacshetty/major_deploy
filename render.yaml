services:
  - type: web
    name: weed-detection
    env: python
    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: gunicorn app:app --timeout 200

    # Render Free Plan default Python is 3.10
    runtime: python
